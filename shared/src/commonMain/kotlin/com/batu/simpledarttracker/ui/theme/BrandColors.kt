package com.batu.simpledarttracker.ui.theme

import androidx.compose.ui.graphics.Color

/** Fixed brand colors — used where the look must never change, such as the logo and splash. */
object Brand {
    val Slate = Color(0xFF0E1512)   // spruce-black background
    val Slate2 = Color(0xFF131C18)  // surface
    val Chalk = Color(0xFFECE6D6)   // chalk white
    val Spruce = Color(0xFF206B45)  // dartboard green · primary
    val Bull = Color(0xFFD8412F)    // bull red · accent
    val Wire = Color(0xFF8A938C)    // spider-wire grey · neutral
    val Key = Color(0xFF243129)     // raised key / card on the arena
}
